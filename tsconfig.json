{
  "$schema": "https://json.schemastore.org/tsconfig",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"],
      "#/*": ["types/*"]
    }
  },
  "include": [
    "apps/**/*.ts",
    "packages/**/*.ts",
  ],
  "exclude": ["node_modules", "dist", "**/*.js"]
}
